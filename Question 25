# Input string
text = input("Enter a string: ")

result = ""

# Convert to uppercase
for ch in text:
    if 'a' <= ch <= 'z':
        result += chr(ord(ch) - 32)
    else:
        result += ch

# Output
print(result)
