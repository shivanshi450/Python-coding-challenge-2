# Input number
num = int(input("Enter a number: "))

original = num
reverse = 0

# Reverse number
while num > 0:
    digit = num % 10
    reverse = reverse * 10 + digit
    num = num // 10

# Check palindrome
if original == reverse:
    print("Palindrome")
else:
    print("Not Palindrome")
