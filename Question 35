# Input list
nums = [3, 1, 2]

# Sorting
n = len(nums)

for i in range(n):
    for j in range(0, n - i - 1):
        if nums[j] > nums[j + 1]:
            # Swap
            nums[j], nums[j + 1] = nums[j + 1], nums[j]

